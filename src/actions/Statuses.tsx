'use server';

import prisma from "../lib/db";
import { revalidatePath } from "next/cache";
import { redirect } from "next/navigation";

// use prisma.update

export async function statusTask(PhaseID: Number, TaskID: Number) {
    
}

export async function status() {
    
}